{
  "name": "promoter-calculator-desktop",
  "version": "0.9.0",
  "private": true,
  "type": "module",
  "license": "GPL-3.0-or-later",
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "build": "node scripts/build.mjs",
    "release": "pnpm build && node scripts/release.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "pnpm build && node --test tests/browser/*.test.mjs",
    "verify": "pnpm test && pnpm reference && pnpm test:browser",
    "reference": "node scripts/compare-reference.mjs"
  },
  "dependencies": {
    "plotly.js-dist-min": "3.1.0"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "esbuild": "0.25.9"
  }
}
